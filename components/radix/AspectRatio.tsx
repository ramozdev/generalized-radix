import * as AspectRatioPrimitive from '@radix-ui/react-aspect-ratio'
import { styled } from 'stitches.config'
import * as styles from './styles/AspectRatio'

const AspectRatio = styled(AspectRatioPrimitive.Root, styles.root)

export { AspectRatio }
